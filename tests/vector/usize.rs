// This file was generated by an associated codegen crate.
// To modify this file, modify the source code of the associated codegen crate.

use core::mem::size_of;

use ggmath::*;

const _: () = assert!(size_of::<Vec2P<usize>>() == size_of::<[usize; 2]>());

#[test]
fn test_vec2_align() {
    assert_eq!(vec2!(0usize, 1usize).align(), vec2!(0usize, 1usize));
}

#[test]
fn test_vec2_pack() {
    assert_eq!(vec2!(0usize, 1usize).pack(), vec2p!(0usize, 1usize));
}

#[test]
fn test_vec2_from_array_as_array() {
    assert_eq!(
        Vec2::from_array([0usize, 1usize]).as_array(),
        [0usize, 1usize]
    );
}

#[test]
fn test_vec2_splat() {
    assert_eq!(Vec2::splat(0usize), vec2!(0usize, 0usize));
}

#[test]
fn test_vec2_index() {
    assert_eq!(vec2!(0usize, 1usize).index(0), 0usize);
    assert_eq!(vec2!(0usize, 1usize).index(1), 1usize);
}

#[test]
#[should_panic]
fn test_vec2_index_panic() {
    vec2!(0usize, 1usize).index(2);
}

#[test]
fn test_vec2_get() {
    assert_eq!(vec2!(0usize, 1usize).get(0), Some(0usize));
    assert_eq!(vec2!(0usize, 1usize).get(1), Some(1usize));

    assert_eq!(vec2!(0usize, 1usize).get(2), None);
}

#[test]
fn test_vec2_get_unchecked() {
    unsafe {
        assert_eq!(vec2!(0usize, 1usize).get_unchecked(0), 0usize);
        assert_eq!(vec2!(0usize, 1usize).get_unchecked(1), 1usize);
    }
}

#[test]
fn test_vec2_set() {
    {
        let mut vec = vec2!(0usize, 1usize);
        vec.set(0, 50usize);

        assert_eq!(vec, vec2!(50usize, 1usize));
    }
    {
        let mut vec = vec2!(0usize, 1usize);
        vec.set(1, 50usize);

        assert_eq!(vec, vec2!(0usize, 50usize));
    }
}

#[test]
#[should_panic]
fn test_vec2_set_panic() {
    let mut vec = vec2!(0usize, 1usize);
    vec.set(2, 0usize);
}

#[test]
fn test_vec2_try_set() {
    {
        let mut vec = vec2!(0usize, 1usize);
        vec.try_set(0, 50usize).unwrap();

        assert_eq!(vec, vec2!(50usize, 1usize));
    }
    {
        let mut vec = vec2!(0usize, 1usize);
        vec.try_set(1, 50usize).unwrap();

        assert_eq!(vec, vec2!(0usize, 50usize));
    }

    assert_eq!(
        vec2!(0usize, 1usize).try_set(2, 0usize),
        Err(IndexOutOfBoundsError)
    );
}

#[test]
fn test_vec2_set_unchecked() {
    unsafe {
        {
            let mut vec = vec2!(0usize, 1usize);
            vec.set_unchecked(0, 50usize);

            assert_eq!(vec, vec2!(50usize, 1usize));
        }
        {
            let mut vec = vec2!(0usize, 1usize);
            vec.set_unchecked(1, 50usize);

            assert_eq!(vec, vec2!(0usize, 50usize));
        }
    }
}

#[test]
fn test_vec2_swizzle() {
    assert_eq!(vec2!(0usize, 1usize).y(), 1usize);
    assert_eq!(vec2!(0usize, 1usize).yx(), vec2!(1usize, 0usize));
    assert_eq!(vec2!(0usize, 1usize).yxy(), vec3!(1usize, 0usize, 1usize));
    assert_eq!(
        vec2!(0usize, 1usize).yxyy(),
        vec4!(1usize, 0usize, 1usize, 1usize)
    );
}

#[test]
fn test_vec2_fold() {
    assert_eq!(
        vec2!(0usize, 1usize).fold(13, |acc, x| acc + x),
        13 + 0usize + 1usize
    );
}

#[test]
fn test_vec2_reduce() {
    assert_eq!(
        vec2!(0usize, 1usize).reduce(|acc, x| acc + x),
        0usize + 1usize
    );
}

#[test]
fn test_vec2_eq_mask() {
    assert_eq!(
        vec2!(0usize, 1usize).eq_mask(vec2!(0usize, 1usize)),
        vec2!(true, true)
    );
    assert_eq!(
        vec2!(0usize, 1usize).eq_mask(vec2!(0usize, 0usize)),
        vec2!(true, false)
    );
    assert_eq!(
        vec2!(0usize, 1usize).eq_mask(vec2!(2usize, 3usize)),
        vec2!(false, false)
    );
}

#[test]
fn test_vec2_ne_mask() {
    assert_eq!(
        vec2!(0usize, 1usize).ne_mask(vec2!(0usize, 1usize)),
        vec2!(false, false)
    );
    assert_eq!(
        vec2!(0usize, 1usize).ne_mask(vec2!(0usize, 0usize)),
        vec2!(false, true)
    );
    assert_eq!(
        vec2!(0usize, 1usize).ne_mask(vec2!(2usize, 3usize)),
        vec2!(true, true)
    );
}

#[test]
fn test_vec2_eq() {
    assert_eq!(vec2!(0usize, 1usize) == vec2!(0usize, 1usize), true);
    assert_eq!(vec2!(0usize, 1usize) == vec2!(0usize, 0usize), false);
    assert_eq!(vec2!(0usize, 1usize) == vec2!(2usize, 3usize), false);
}

#[test]
fn test_vec2_ne() {
    assert_eq!(vec2!(0usize, 1usize) != vec2!(0usize, 1usize), false);
    assert_eq!(vec2!(0usize, 1usize) != vec2!(0usize, 0usize), true);
    assert_eq!(vec2!(0usize, 1usize) != vec2!(2usize, 3usize), true);
}

#[test]
fn test_vec2_debug() {
    assert_eq!(format!("{:?}", vec2!(0usize, 1usize)), "(0, 1)");
}

#[test]
fn test_vec2_display() {
    assert_eq!(format!("{}", vec2!(0usize, 1usize)), "(0, 1)");
}

#[test]
fn test_vec2p_align() {
    assert_eq!(vec2p!(0usize, 1usize).align(), vec2!(0usize, 1usize));
}

#[test]
fn test_vec2p_pack() {
    assert_eq!(vec2p!(0usize, 1usize).pack(), vec2p!(0usize, 1usize));
}

#[test]
fn test_vec2p_from_array_as_array() {
    assert_eq!(
        Vec2P::from_array([0usize, 1usize]).as_array(),
        [0usize, 1usize]
    );
}

#[test]
fn test_vec2p_splat() {
    assert_eq!(Vec2P::splat(0usize), vec2p!(0usize, 0usize));
}

#[test]
fn test_vec2p_index() {
    assert_eq!(vec2p!(0usize, 1usize).index(0), 0usize);
    assert_eq!(vec2p!(0usize, 1usize).index(1), 1usize);
}

#[test]
#[should_panic]
fn test_vec2p_index_panic() {
    vec2p!(0usize, 1usize).index(2);
}

#[test]
fn test_vec2p_get() {
    assert_eq!(vec2p!(0usize, 1usize).get(0), Some(0usize));
    assert_eq!(vec2p!(0usize, 1usize).get(1), Some(1usize));

    assert_eq!(vec2p!(0usize, 1usize).get(2), None);
}

#[test]
fn test_vec2p_get_unchecked() {
    unsafe {
        assert_eq!(vec2p!(0usize, 1usize).get_unchecked(0), 0usize);
        assert_eq!(vec2p!(0usize, 1usize).get_unchecked(1), 1usize);
    }
}

#[test]
fn test_vec2p_set() {
    {
        let mut vec = vec2p!(0usize, 1usize);
        vec.set(0, 50usize);

        assert_eq!(vec, vec2p!(50usize, 1usize));
    }
    {
        let mut vec = vec2p!(0usize, 1usize);
        vec.set(1, 50usize);

        assert_eq!(vec, vec2p!(0usize, 50usize));
    }
}

#[test]
#[should_panic]
fn test_vec2p_set_panic() {
    let mut vec = vec2p!(0usize, 1usize);
    vec.set(2, 0usize);
}

#[test]
fn test_vec2p_try_set() {
    {
        let mut vec = vec2p!(0usize, 1usize);
        vec.try_set(0, 50usize).unwrap();

        assert_eq!(vec, vec2p!(50usize, 1usize));
    }
    {
        let mut vec = vec2p!(0usize, 1usize);
        vec.try_set(1, 50usize).unwrap();

        assert_eq!(vec, vec2p!(0usize, 50usize));
    }

    assert_eq!(
        vec2p!(0usize, 1usize).try_set(2, 0usize),
        Err(IndexOutOfBoundsError)
    );
}

#[test]
fn test_vec2p_set_unchecked() {
    unsafe {
        {
            let mut vec = vec2p!(0usize, 1usize);
            vec.set_unchecked(0, 50usize);

            assert_eq!(vec, vec2p!(50usize, 1usize));
        }
        {
            let mut vec = vec2p!(0usize, 1usize);
            vec.set_unchecked(1, 50usize);

            assert_eq!(vec, vec2p!(0usize, 50usize));
        }
    }
}

#[test]
fn test_vec2p_swizzle() {
    assert_eq!(vec2p!(0usize, 1usize).y(), 1usize);
    assert_eq!(vec2p!(0usize, 1usize).yx(), vec2p!(1usize, 0usize));
    assert_eq!(vec2p!(0usize, 1usize).yxy(), vec3p!(1usize, 0usize, 1usize));
    assert_eq!(
        vec2p!(0usize, 1usize).yxyy(),
        vec4p!(1usize, 0usize, 1usize, 1usize)
    );
}

#[test]
fn test_vec2p_fold() {
    assert_eq!(
        vec2p!(0usize, 1usize).fold(13, |acc, x| acc + x),
        13 + 0usize + 1usize
    );
}

#[test]
fn test_vec2p_reduce() {
    assert_eq!(
        vec2p!(0usize, 1usize).reduce(|acc, x| acc + x),
        0usize + 1usize
    );
}

#[test]
fn test_vec2p_eq_mask() {
    assert_eq!(
        vec2p!(0usize, 1usize).eq_mask(vec2p!(0usize, 1usize)),
        vec2p!(true, true)
    );
    assert_eq!(
        vec2p!(0usize, 1usize).eq_mask(vec2p!(0usize, 0usize)),
        vec2p!(true, false)
    );
    assert_eq!(
        vec2p!(0usize, 1usize).eq_mask(vec2p!(2usize, 3usize)),
        vec2p!(false, false)
    );
}

#[test]
fn test_vec2p_ne_mask() {
    assert_eq!(
        vec2p!(0usize, 1usize).ne_mask(vec2p!(0usize, 1usize)),
        vec2p!(false, false)
    );
    assert_eq!(
        vec2p!(0usize, 1usize).ne_mask(vec2p!(0usize, 0usize)),
        vec2p!(false, true)
    );
    assert_eq!(
        vec2p!(0usize, 1usize).ne_mask(vec2p!(2usize, 3usize)),
        vec2p!(true, true)
    );
}

#[test]
fn test_vec2p_eq() {
    assert_eq!(vec2p!(0usize, 1usize) == vec2p!(0usize, 1usize), true);
    assert_eq!(vec2p!(0usize, 1usize) == vec2p!(0usize, 0usize), false);
    assert_eq!(vec2p!(0usize, 1usize) == vec2p!(2usize, 3usize), false);
}

#[test]
fn test_vec2p_ne() {
    assert_eq!(vec2p!(0usize, 1usize) != vec2p!(0usize, 1usize), false);
    assert_eq!(vec2p!(0usize, 1usize) != vec2p!(0usize, 0usize), true);
    assert_eq!(vec2p!(0usize, 1usize) != vec2p!(2usize, 3usize), true);
}

#[test]
fn test_vec2p_debug() {
    assert_eq!(format!("{:?}", vec2p!(0usize, 1usize)), "(0, 1)");
}

#[test]
fn test_vec2p_display() {
    assert_eq!(format!("{}", vec2p!(0usize, 1usize)), "(0, 1)");
}

const _: () = assert!(size_of::<Vec3P<usize>>() == size_of::<[usize; 3]>());

#[test]
fn test_vec3_align() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).align(),
        vec3!(0usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec3_pack() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).pack(),
        vec3p!(0usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec3_from_array_as_array() {
    assert_eq!(
        Vec3::from_array([0usize, 1usize, 2usize]).as_array(),
        [0usize, 1usize, 2usize]
    );
}

#[test]
fn test_vec3_splat() {
    assert_eq!(Vec3::splat(0usize), vec3!(0usize, 0usize, 0usize));
}

#[test]
fn test_vec3_index() {
    assert_eq!(vec3!(0usize, 1usize, 2usize).index(0), 0usize);
    assert_eq!(vec3!(0usize, 1usize, 2usize).index(1), 1usize);
    assert_eq!(vec3!(0usize, 1usize, 2usize).index(2), 2usize);
}

#[test]
#[should_panic]
fn test_vec3_index_panic() {
    vec3!(0usize, 1usize, 2usize).index(3);
}

#[test]
fn test_vec3_get() {
    assert_eq!(vec3!(0usize, 1usize, 2usize).get(0), Some(0usize));
    assert_eq!(vec3!(0usize, 1usize, 2usize).get(1), Some(1usize));
    assert_eq!(vec3!(0usize, 1usize, 2usize).get(2), Some(2usize));

    assert_eq!(vec3!(0usize, 1usize, 2usize).get(3), None);
}

#[test]
fn test_vec3_get_unchecked() {
    unsafe {
        assert_eq!(vec3!(0usize, 1usize, 2usize).get_unchecked(0), 0usize);
        assert_eq!(vec3!(0usize, 1usize, 2usize).get_unchecked(1), 1usize);
        assert_eq!(vec3!(0usize, 1usize, 2usize).get_unchecked(2), 2usize);
    }
}

#[test]
fn test_vec3_set() {
    {
        let mut vec = vec3!(0usize, 1usize, 2usize);
        vec.set(0, 50usize);

        assert_eq!(vec, vec3!(50usize, 1usize, 2usize));
    }
    {
        let mut vec = vec3!(0usize, 1usize, 2usize);
        vec.set(1, 50usize);

        assert_eq!(vec, vec3!(0usize, 50usize, 2usize));
    }
    {
        let mut vec = vec3!(0usize, 1usize, 2usize);
        vec.set(2, 50usize);

        assert_eq!(vec, vec3!(0usize, 1usize, 50usize));
    }
}

#[test]
#[should_panic]
fn test_vec3_set_panic() {
    let mut vec = vec3!(0usize, 1usize, 2usize);
    vec.set(3, 0usize);
}

#[test]
fn test_vec3_try_set() {
    {
        let mut vec = vec3!(0usize, 1usize, 2usize);
        vec.try_set(0, 50usize).unwrap();

        assert_eq!(vec, vec3!(50usize, 1usize, 2usize));
    }
    {
        let mut vec = vec3!(0usize, 1usize, 2usize);
        vec.try_set(1, 50usize).unwrap();

        assert_eq!(vec, vec3!(0usize, 50usize, 2usize));
    }
    {
        let mut vec = vec3!(0usize, 1usize, 2usize);
        vec.try_set(2, 50usize).unwrap();

        assert_eq!(vec, vec3!(0usize, 1usize, 50usize));
    }

    assert_eq!(
        vec3!(0usize, 1usize, 2usize).try_set(3, 0usize),
        Err(IndexOutOfBoundsError)
    );
}

#[test]
fn test_vec3_set_unchecked() {
    unsafe {
        {
            let mut vec = vec3!(0usize, 1usize, 2usize);
            vec.set_unchecked(0, 50usize);

            assert_eq!(vec, vec3!(50usize, 1usize, 2usize));
        }
        {
            let mut vec = vec3!(0usize, 1usize, 2usize);
            vec.set_unchecked(1, 50usize);

            assert_eq!(vec, vec3!(0usize, 50usize, 2usize));
        }
        {
            let mut vec = vec3!(0usize, 1usize, 2usize);
            vec.set_unchecked(2, 50usize);

            assert_eq!(vec, vec3!(0usize, 1usize, 50usize));
        }
    }
}

#[test]
fn test_vec3_swizzle() {
    assert_eq!(vec3!(0usize, 1usize, 2usize).z(), 2usize);
    assert_eq!(vec3!(0usize, 1usize, 2usize).zx(), vec2!(2usize, 0usize));
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).zxy(),
        vec3!(2usize, 0usize, 1usize)
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).zxyz(),
        vec4!(2usize, 0usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec3_fold() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).fold(13, |acc, x| acc + x),
        13 + 0usize + 1usize + 2usize
    );
}

#[test]
fn test_vec3_reduce() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).reduce(|acc, x| acc + x),
        0usize + 1usize + 2usize
    );
}

#[test]
fn test_vec3_eq_mask() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).eq_mask(vec3!(0usize, 1usize, 2usize)),
        vec3!(true, true, true)
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).eq_mask(vec3!(0usize, 0usize, 2usize)),
        vec3!(true, false, true)
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).eq_mask(vec3!(3usize, 4usize, 5usize)),
        vec3!(false, false, false)
    );
}

#[test]
fn test_vec3_ne_mask() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).ne_mask(vec3!(0usize, 1usize, 2usize)),
        vec3!(false, false, false)
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).ne_mask(vec3!(0usize, 0usize, 2usize)),
        vec3!(false, true, false)
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize).ne_mask(vec3!(3usize, 4usize, 5usize)),
        vec3!(true, true, true)
    );
}

#[test]
fn test_vec3_eq() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize) == vec3!(0usize, 1usize, 2usize),
        true
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize) == vec3!(0usize, 0usize, 2usize),
        false
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize) == vec3!(3usize, 4usize, 5usize),
        false
    );
}

#[test]
fn test_vec3_ne() {
    assert_eq!(
        vec3!(0usize, 1usize, 2usize) != vec3!(0usize, 1usize, 2usize),
        false
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize) != vec3!(0usize, 0usize, 2usize),
        true
    );
    assert_eq!(
        vec3!(0usize, 1usize, 2usize) != vec3!(3usize, 4usize, 5usize),
        true
    );
}

#[test]
fn test_vec3_debug() {
    assert_eq!(format!("{:?}", vec3!(0usize, 1usize, 2usize)), "(0, 1, 2)");
}

#[test]
fn test_vec3_display() {
    assert_eq!(format!("{}", vec3!(0usize, 1usize, 2usize)), "(0, 1, 2)");
}

#[test]
fn test_vec3p_align() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).align(),
        vec3!(0usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec3p_pack() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).pack(),
        vec3p!(0usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec3p_from_array_as_array() {
    assert_eq!(
        Vec3P::from_array([0usize, 1usize, 2usize]).as_array(),
        [0usize, 1usize, 2usize]
    );
}

#[test]
fn test_vec3p_splat() {
    assert_eq!(Vec3P::splat(0usize), vec3p!(0usize, 0usize, 0usize));
}

#[test]
fn test_vec3p_index() {
    assert_eq!(vec3p!(0usize, 1usize, 2usize).index(0), 0usize);
    assert_eq!(vec3p!(0usize, 1usize, 2usize).index(1), 1usize);
    assert_eq!(vec3p!(0usize, 1usize, 2usize).index(2), 2usize);
}

#[test]
#[should_panic]
fn test_vec3p_index_panic() {
    vec3p!(0usize, 1usize, 2usize).index(3);
}

#[test]
fn test_vec3p_get() {
    assert_eq!(vec3p!(0usize, 1usize, 2usize).get(0), Some(0usize));
    assert_eq!(vec3p!(0usize, 1usize, 2usize).get(1), Some(1usize));
    assert_eq!(vec3p!(0usize, 1usize, 2usize).get(2), Some(2usize));

    assert_eq!(vec3p!(0usize, 1usize, 2usize).get(3), None);
}

#[test]
fn test_vec3p_get_unchecked() {
    unsafe {
        assert_eq!(vec3p!(0usize, 1usize, 2usize).get_unchecked(0), 0usize);
        assert_eq!(vec3p!(0usize, 1usize, 2usize).get_unchecked(1), 1usize);
        assert_eq!(vec3p!(0usize, 1usize, 2usize).get_unchecked(2), 2usize);
    }
}

#[test]
fn test_vec3p_set() {
    {
        let mut vec = vec3p!(0usize, 1usize, 2usize);
        vec.set(0, 50usize);

        assert_eq!(vec, vec3p!(50usize, 1usize, 2usize));
    }
    {
        let mut vec = vec3p!(0usize, 1usize, 2usize);
        vec.set(1, 50usize);

        assert_eq!(vec, vec3p!(0usize, 50usize, 2usize));
    }
    {
        let mut vec = vec3p!(0usize, 1usize, 2usize);
        vec.set(2, 50usize);

        assert_eq!(vec, vec3p!(0usize, 1usize, 50usize));
    }
}

#[test]
#[should_panic]
fn test_vec3p_set_panic() {
    let mut vec = vec3p!(0usize, 1usize, 2usize);
    vec.set(3, 0usize);
}

#[test]
fn test_vec3p_try_set() {
    {
        let mut vec = vec3p!(0usize, 1usize, 2usize);
        vec.try_set(0, 50usize).unwrap();

        assert_eq!(vec, vec3p!(50usize, 1usize, 2usize));
    }
    {
        let mut vec = vec3p!(0usize, 1usize, 2usize);
        vec.try_set(1, 50usize).unwrap();

        assert_eq!(vec, vec3p!(0usize, 50usize, 2usize));
    }
    {
        let mut vec = vec3p!(0usize, 1usize, 2usize);
        vec.try_set(2, 50usize).unwrap();

        assert_eq!(vec, vec3p!(0usize, 1usize, 50usize));
    }

    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).try_set(3, 0usize),
        Err(IndexOutOfBoundsError)
    );
}

#[test]
fn test_vec3p_set_unchecked() {
    unsafe {
        {
            let mut vec = vec3p!(0usize, 1usize, 2usize);
            vec.set_unchecked(0, 50usize);

            assert_eq!(vec, vec3p!(50usize, 1usize, 2usize));
        }
        {
            let mut vec = vec3p!(0usize, 1usize, 2usize);
            vec.set_unchecked(1, 50usize);

            assert_eq!(vec, vec3p!(0usize, 50usize, 2usize));
        }
        {
            let mut vec = vec3p!(0usize, 1usize, 2usize);
            vec.set_unchecked(2, 50usize);

            assert_eq!(vec, vec3p!(0usize, 1usize, 50usize));
        }
    }
}

#[test]
fn test_vec3p_swizzle() {
    assert_eq!(vec3p!(0usize, 1usize, 2usize).z(), 2usize);
    assert_eq!(vec3p!(0usize, 1usize, 2usize).zx(), vec2p!(2usize, 0usize));
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).zxy(),
        vec3p!(2usize, 0usize, 1usize)
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).zxyz(),
        vec4p!(2usize, 0usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec3p_fold() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).fold(13, |acc, x| acc + x),
        13 + 0usize + 1usize + 2usize
    );
}

#[test]
fn test_vec3p_reduce() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).reduce(|acc, x| acc + x),
        0usize + 1usize + 2usize
    );
}

#[test]
fn test_vec3p_eq_mask() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).eq_mask(vec3p!(0usize, 1usize, 2usize)),
        vec3p!(true, true, true)
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).eq_mask(vec3p!(0usize, 0usize, 2usize)),
        vec3p!(true, false, true)
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).eq_mask(vec3p!(3usize, 4usize, 5usize)),
        vec3p!(false, false, false)
    );
}

#[test]
fn test_vec3p_ne_mask() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).ne_mask(vec3p!(0usize, 1usize, 2usize)),
        vec3p!(false, false, false)
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).ne_mask(vec3p!(0usize, 0usize, 2usize)),
        vec3p!(false, true, false)
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize).ne_mask(vec3p!(3usize, 4usize, 5usize)),
        vec3p!(true, true, true)
    );
}

#[test]
fn test_vec3p_eq() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize) == vec3p!(0usize, 1usize, 2usize),
        true
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize) == vec3p!(0usize, 0usize, 2usize),
        false
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize) == vec3p!(3usize, 4usize, 5usize),
        false
    );
}

#[test]
fn test_vec3p_ne() {
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize) != vec3p!(0usize, 1usize, 2usize),
        false
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize) != vec3p!(0usize, 0usize, 2usize),
        true
    );
    assert_eq!(
        vec3p!(0usize, 1usize, 2usize) != vec3p!(3usize, 4usize, 5usize),
        true
    );
}

#[test]
fn test_vec3p_debug() {
    assert_eq!(format!("{:?}", vec3p!(0usize, 1usize, 2usize)), "(0, 1, 2)");
}

#[test]
fn test_vec3p_display() {
    assert_eq!(format!("{}", vec3p!(0usize, 1usize, 2usize)), "(0, 1, 2)");
}

const _: () = assert!(size_of::<Vec4P<usize>>() == size_of::<[usize; 4]>());

#[test]
fn test_vec4_align() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).align(),
        vec4!(0usize, 1usize, 2usize, 3usize)
    );
}

#[test]
fn test_vec4_pack() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).pack(),
        vec4p!(0usize, 1usize, 2usize, 3usize)
    );
}

#[test]
fn test_vec4_from_array_as_array() {
    assert_eq!(
        Vec4::from_array([0usize, 1usize, 2usize, 3usize]).as_array(),
        [0usize, 1usize, 2usize, 3usize]
    );
}

#[test]
fn test_vec4_splat() {
    assert_eq!(Vec4::splat(0usize), vec4!(0usize, 0usize, 0usize, 0usize));
}

#[test]
fn test_vec4_index() {
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).index(0), 0usize);
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).index(1), 1usize);
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).index(2), 2usize);
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).index(3), 3usize);
}

#[test]
#[should_panic]
fn test_vec4_index_panic() {
    vec4!(0usize, 1usize, 2usize, 3usize).index(4);
}

#[test]
fn test_vec4_get() {
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).get(0), Some(0usize));
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).get(1), Some(1usize));
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).get(2), Some(2usize));
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).get(3), Some(3usize));

    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).get(4), None);
}

#[test]
fn test_vec4_get_unchecked() {
    unsafe {
        assert_eq!(
            vec4!(0usize, 1usize, 2usize, 3usize).get_unchecked(0),
            0usize
        );
        assert_eq!(
            vec4!(0usize, 1usize, 2usize, 3usize).get_unchecked(1),
            1usize
        );
        assert_eq!(
            vec4!(0usize, 1usize, 2usize, 3usize).get_unchecked(2),
            2usize
        );
        assert_eq!(
            vec4!(0usize, 1usize, 2usize, 3usize).get_unchecked(3),
            3usize
        );
    }
}

#[test]
fn test_vec4_set() {
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.set(0, 50usize);

        assert_eq!(vec, vec4!(50usize, 1usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.set(1, 50usize);

        assert_eq!(vec, vec4!(0usize, 50usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.set(2, 50usize);

        assert_eq!(vec, vec4!(0usize, 1usize, 50usize, 3usize));
    }
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.set(3, 50usize);

        assert_eq!(vec, vec4!(0usize, 1usize, 2usize, 50usize));
    }
}

#[test]
#[should_panic]
fn test_vec4_set_panic() {
    let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
    vec.set(4, 0usize);
}

#[test]
fn test_vec4_try_set() {
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(0, 50usize).unwrap();

        assert_eq!(vec, vec4!(50usize, 1usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(1, 50usize).unwrap();

        assert_eq!(vec, vec4!(0usize, 50usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(2, 50usize).unwrap();

        assert_eq!(vec, vec4!(0usize, 1usize, 50usize, 3usize));
    }
    {
        let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(3, 50usize).unwrap();

        assert_eq!(vec, vec4!(0usize, 1usize, 2usize, 50usize));
    }

    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).try_set(4, 0usize),
        Err(IndexOutOfBoundsError)
    );
}

#[test]
fn test_vec4_set_unchecked() {
    unsafe {
        {
            let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(0, 50usize);

            assert_eq!(vec, vec4!(50usize, 1usize, 2usize, 3usize));
        }
        {
            let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(1, 50usize);

            assert_eq!(vec, vec4!(0usize, 50usize, 2usize, 3usize));
        }
        {
            let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(2, 50usize);

            assert_eq!(vec, vec4!(0usize, 1usize, 50usize, 3usize));
        }
        {
            let mut vec = vec4!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(3, 50usize);

            assert_eq!(vec, vec4!(0usize, 1usize, 2usize, 50usize));
        }
    }
}

#[test]
fn test_vec4_swizzle() {
    assert_eq!(vec4!(0usize, 1usize, 2usize, 3usize).z(), 2usize);
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).zw(),
        vec2!(2usize, 3usize)
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).zwy(),
        vec3!(2usize, 3usize, 1usize)
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).zwyz(),
        vec4!(2usize, 3usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec4_fold() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).fold(13, |acc, x| acc + x),
        13 + 0usize + 1usize + 2usize + 3usize
    );
}

#[test]
fn test_vec4_reduce() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).reduce(|acc, x| acc + x),
        0usize + 1usize + 2usize + 3usize
    );
}

#[test]
fn test_vec4_eq_mask() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).eq_mask(vec4!(0usize, 1usize, 2usize, 3usize)),
        vec4!(true, true, true, true)
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).eq_mask(vec4!(0usize, 0usize, 2usize, 3usize)),
        vec4!(true, false, true, true)
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).eq_mask(vec4!(4usize, 5usize, 6usize, 7usize)),
        vec4!(false, false, false, false)
    );
}

#[test]
fn test_vec4_ne_mask() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).ne_mask(vec4!(0usize, 1usize, 2usize, 3usize)),
        vec4!(false, false, false, false)
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).ne_mask(vec4!(0usize, 0usize, 2usize, 3usize)),
        vec4!(false, true, false, false)
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize).ne_mask(vec4!(4usize, 5usize, 6usize, 7usize)),
        vec4!(true, true, true, true)
    );
}

#[test]
fn test_vec4_eq() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize) == vec4!(0usize, 1usize, 2usize, 3usize),
        true
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize) == vec4!(0usize, 0usize, 2usize, 3usize),
        false
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize) == vec4!(4usize, 5usize, 6usize, 7usize),
        false
    );
}

#[test]
fn test_vec4_ne() {
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize) != vec4!(0usize, 1usize, 2usize, 3usize),
        false
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize) != vec4!(0usize, 0usize, 2usize, 3usize),
        true
    );
    assert_eq!(
        vec4!(0usize, 1usize, 2usize, 3usize) != vec4!(4usize, 5usize, 6usize, 7usize),
        true
    );
}

#[test]
fn test_vec4_debug() {
    assert_eq!(
        format!("{:?}", vec4!(0usize, 1usize, 2usize, 3usize)),
        "(0, 1, 2, 3)"
    );
}

#[test]
fn test_vec4_display() {
    assert_eq!(
        format!("{}", vec4!(0usize, 1usize, 2usize, 3usize)),
        "(0, 1, 2, 3)"
    );
}

#[test]
fn test_vec4p_align() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).align(),
        vec4!(0usize, 1usize, 2usize, 3usize)
    );
}

#[test]
fn test_vec4p_pack() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).pack(),
        vec4p!(0usize, 1usize, 2usize, 3usize)
    );
}

#[test]
fn test_vec4p_from_array_as_array() {
    assert_eq!(
        Vec4P::from_array([0usize, 1usize, 2usize, 3usize]).as_array(),
        [0usize, 1usize, 2usize, 3usize]
    );
}

#[test]
fn test_vec4p_splat() {
    assert_eq!(Vec4P::splat(0usize), vec4p!(0usize, 0usize, 0usize, 0usize));
}

#[test]
fn test_vec4p_index() {
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).index(0), 0usize);
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).index(1), 1usize);
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).index(2), 2usize);
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).index(3), 3usize);
}

#[test]
#[should_panic]
fn test_vec4p_index_panic() {
    vec4p!(0usize, 1usize, 2usize, 3usize).index(4);
}

#[test]
fn test_vec4p_get() {
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).get(0), Some(0usize));
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).get(1), Some(1usize));
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).get(2), Some(2usize));
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).get(3), Some(3usize));

    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).get(4), None);
}

#[test]
fn test_vec4p_get_unchecked() {
    unsafe {
        assert_eq!(
            vec4p!(0usize, 1usize, 2usize, 3usize).get_unchecked(0),
            0usize
        );
        assert_eq!(
            vec4p!(0usize, 1usize, 2usize, 3usize).get_unchecked(1),
            1usize
        );
        assert_eq!(
            vec4p!(0usize, 1usize, 2usize, 3usize).get_unchecked(2),
            2usize
        );
        assert_eq!(
            vec4p!(0usize, 1usize, 2usize, 3usize).get_unchecked(3),
            3usize
        );
    }
}

#[test]
fn test_vec4p_set() {
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.set(0, 50usize);

        assert_eq!(vec, vec4p!(50usize, 1usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.set(1, 50usize);

        assert_eq!(vec, vec4p!(0usize, 50usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.set(2, 50usize);

        assert_eq!(vec, vec4p!(0usize, 1usize, 50usize, 3usize));
    }
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.set(3, 50usize);

        assert_eq!(vec, vec4p!(0usize, 1usize, 2usize, 50usize));
    }
}

#[test]
#[should_panic]
fn test_vec4p_set_panic() {
    let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
    vec.set(4, 0usize);
}

#[test]
fn test_vec4p_try_set() {
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(0, 50usize).unwrap();

        assert_eq!(vec, vec4p!(50usize, 1usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(1, 50usize).unwrap();

        assert_eq!(vec, vec4p!(0usize, 50usize, 2usize, 3usize));
    }
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(2, 50usize).unwrap();

        assert_eq!(vec, vec4p!(0usize, 1usize, 50usize, 3usize));
    }
    {
        let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
        vec.try_set(3, 50usize).unwrap();

        assert_eq!(vec, vec4p!(0usize, 1usize, 2usize, 50usize));
    }

    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).try_set(4, 0usize),
        Err(IndexOutOfBoundsError)
    );
}

#[test]
fn test_vec4p_set_unchecked() {
    unsafe {
        {
            let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(0, 50usize);

            assert_eq!(vec, vec4p!(50usize, 1usize, 2usize, 3usize));
        }
        {
            let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(1, 50usize);

            assert_eq!(vec, vec4p!(0usize, 50usize, 2usize, 3usize));
        }
        {
            let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(2, 50usize);

            assert_eq!(vec, vec4p!(0usize, 1usize, 50usize, 3usize));
        }
        {
            let mut vec = vec4p!(0usize, 1usize, 2usize, 3usize);
            vec.set_unchecked(3, 50usize);

            assert_eq!(vec, vec4p!(0usize, 1usize, 2usize, 50usize));
        }
    }
}

#[test]
fn test_vec4p_swizzle() {
    assert_eq!(vec4p!(0usize, 1usize, 2usize, 3usize).z(), 2usize);
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).zw(),
        vec2p!(2usize, 3usize)
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).zwy(),
        vec3p!(2usize, 3usize, 1usize)
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).zwyz(),
        vec4p!(2usize, 3usize, 1usize, 2usize)
    );
}

#[test]
fn test_vec4p_fold() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).fold(13, |acc, x| acc + x),
        13 + 0usize + 1usize + 2usize + 3usize
    );
}

#[test]
fn test_vec4p_reduce() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).reduce(|acc, x| acc + x),
        0usize + 1usize + 2usize + 3usize
    );
}

#[test]
fn test_vec4p_eq_mask() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).eq_mask(vec4p!(0usize, 1usize, 2usize, 3usize)),
        vec4p!(true, true, true, true)
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).eq_mask(vec4p!(0usize, 0usize, 2usize, 3usize)),
        vec4p!(true, false, true, true)
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).eq_mask(vec4p!(4usize, 5usize, 6usize, 7usize)),
        vec4p!(false, false, false, false)
    );
}

#[test]
fn test_vec4p_ne_mask() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).ne_mask(vec4p!(0usize, 1usize, 2usize, 3usize)),
        vec4p!(false, false, false, false)
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).ne_mask(vec4p!(0usize, 0usize, 2usize, 3usize)),
        vec4p!(false, true, false, false)
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize).ne_mask(vec4p!(4usize, 5usize, 6usize, 7usize)),
        vec4p!(true, true, true, true)
    );
}

#[test]
fn test_vec4p_eq() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize) == vec4p!(0usize, 1usize, 2usize, 3usize),
        true
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize) == vec4p!(0usize, 0usize, 2usize, 3usize),
        false
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize) == vec4p!(4usize, 5usize, 6usize, 7usize),
        false
    );
}

#[test]
fn test_vec4p_ne() {
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize) != vec4p!(0usize, 1usize, 2usize, 3usize),
        false
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize) != vec4p!(0usize, 0usize, 2usize, 3usize),
        true
    );
    assert_eq!(
        vec4p!(0usize, 1usize, 2usize, 3usize) != vec4p!(4usize, 5usize, 6usize, 7usize),
        true
    );
}

#[test]
fn test_vec4p_debug() {
    assert_eq!(
        format!("{:?}", vec4p!(0usize, 1usize, 2usize, 3usize)),
        "(0, 1, 2, 3)"
    );
}

#[test]
fn test_vec4p_display() {
    assert_eq!(
        format!("{}", vec4p!(0usize, 1usize, 2usize, 3usize)),
        "(0, 1, 2, 3)"
    );
}
