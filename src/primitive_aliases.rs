/// Type-aliases for `f32` types.
pub mod f32_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type F => f32);
}

/// Type-aliases for `f64` types.
pub mod f64_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type D => f64);
}

/// Type-aliases for `i16` types.
pub mod i8_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type I8 => i8);
}

/// Type-aliases for `i16` types.
pub mod i16_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type I16 => i16);
}

/// Type-aliases for `i32` types.
pub mod i32_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type I => i32);
}

/// Type-aliases for `i64` types.
pub mod i64_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type I64 => i64);
}

/// Type-aliases for `i128` types.
pub mod i128_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type I128 => i128);
}

/// Type-aliases for `isize` types.
pub mod isize_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type Isize => isize);
}

/// Type-aliases for `u8` types.
pub mod u8_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type U8 => u8);
}

/// Type-aliases for `u16` types.
pub mod u16_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type U16 => u16);
}

/// Type-aliases for `u32` types.
pub mod u32_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type U => u32);
}

/// Type-aliases for `u64` types.
pub mod u64_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type U64 => u64);
}

/// Type-aliases for `u128` types.
pub mod u128_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type U128 => u128);
}

/// Type-aliases for `usize` types.
pub mod usize_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type Usize => usize);
}

/// Type-aliases for `bool` types.
pub mod bool_aliases {
    use crate::declare_vector_aliases;

    declare_vector_aliases!(pub type B => bool);
}
